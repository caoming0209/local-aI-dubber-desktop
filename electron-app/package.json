{
  "name": "ai-dubber-desktop",
  "version": "1.0.0",
  "description": "智影口播 · AI数字人视频助手",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "tsc -b && electron .",
    "build": "tsc -b",
    "build:win": "tsc -b && electron-builder --win",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1",
    "electron-updater": "^6.8.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "electron": "^40.6.0",
    "electron-builder": "^25.1.8",
    "typescript": "~5.8.2"
  }
}
